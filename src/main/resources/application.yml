server:
  port: 43376

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: account-service
          uri: lb://account-service
          predicates:
            - Path=/accounts/**
        - id: client-service
          uri: lb://client-service
          predicates:
            - Path=/clients/**
        - id: provider-service
          uri: lb://provider-service
          predicates:
            - Path=/providers/**
        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/orders/**
        - id: order-service-java
          uri: lb://order-service-java
          predicates:
            - Path=/java/orders/**

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
    instance:
      hostname: localhost
